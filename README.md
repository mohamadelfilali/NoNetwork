# NoNetwork (Prototype)

نسخة بداية لمشروع NoNetwork — واجهة أدوات ومقالات بنظام حسابات عبر Firebase (Google sign-in)، مع خيار مستقبلي لربط Discord (مُعلّق حالياً).

## تشغيل محلي
1. انسخ المستودع:
   ```bash
   git clone <repo-url>
   cd NoNetwork
